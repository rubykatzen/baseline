name: Notify Telegram unreleased commits
on:
  schedule:
    - cron: "0 22 * * *"
  workflow_dispatch:
jobs:
  notify:
    uses: rubykatzen/releaser/.github/workflows/notify-telegram-unreleased-shared.yml@v0.5.6
    secrets: inherit
