name: Lint
on:
  push:
    branches: ["main"]
  pull_request:
jobs:
  lint:
    uses: ./.github/workflows/lint-shared.yml
    with:
      linters: yamllint, pymarkdown, shellcheck, actionlint, rubocop
