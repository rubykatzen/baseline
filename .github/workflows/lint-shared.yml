name: Lint (shared)
on:
  workflow_call:
    inputs:
      exclude:
        description: JSON array of linters to exclude from automatic selection.
        type: string
        default: "[]"
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: rubykatzen/baseline/.github/actions/detect-linters@v0.8
        id: detect
        with:
          exclude: ${{ inputs.exclude }}
      - uses: rubykatzen/baseline/.github/actions/setup-runtimes@v0.8
        with:
          linters: ${{ steps.detect.outputs.linters }}
      - uses: rubykatzen/baseline/.github/actions/check-precommit@v0.8
        if: contains(fromJSON(steps.detect.outputs.linters), 'pre-commit')
        with:
          linters: ${{ steps.detect.outputs.linters }}
      - uses: rubykatzen/baseline/.github/actions/lint-yamllint@v0.8
        if: contains(fromJSON(steps.detect.outputs.linters), 'yamllint')
      - uses: rubykatzen/baseline/.github/actions/lint-pymarkdown@v0.8
        if: contains(fromJSON(steps.detect.outputs.linters), 'pymarkdown')
      - uses: rubykatzen/baseline/.github/actions/lint-ruff@v0.8
        if: contains(fromJSON(steps.detect.outputs.linters), 'ruff')
      - uses: rubykatzen/baseline/.github/actions/lint-shellcheck@v0.8
        if: contains(fromJSON(steps.detect.outputs.linters), 'shellcheck')
      - uses: rubykatzen/baseline/.github/actions/lint-actionlint@v0.8
        if: contains(fromJSON(steps.detect.outputs.linters), 'actionlint')
      - uses: rubykatzen/baseline/.github/actions/lint-rubocop@v0.8
        if: contains(fromJSON(steps.detect.outputs.linters), 'rubocop')
      - uses: rubykatzen/baseline/.github/actions/lint-erb-lint@v0.8
        if: contains(fromJSON(steps.detect.outputs.linters), 'erb-lint')
      - uses: rubykatzen/baseline/.github/actions/lint-herb@v0.8
        if: contains(fromJSON(steps.detect.outputs.linters), 'herb')
