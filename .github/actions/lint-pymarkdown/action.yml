name: pymarkdown
description: Lint Markdown files using the canonical pymarkdown config from baseline.
runs:
  using: composite
  steps:
    - run: pip install pymarkdownlnt
      shell: bash
    - run: pymarkdown --config "${{ github.action_path }}/../../../config/pymarkdown.json" scan .
      shell: bash
