name: pymarkdown
description: Lint Markdown files using installed pymarkdown and the canonical baseline config.
runs:
  using: composite
  steps:
    - run: pymarkdown --config "${{ github.action_path }}/../../../config/pymarkdown.json" scan .
      shell: bash
