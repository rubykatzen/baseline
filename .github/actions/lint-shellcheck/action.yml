name: shellcheck
description: Lint shell scripts using installed shellcheck and the canonical baseline config.
runs:
  using: composite
  steps:
    - run: "${{ github.action_path }}/../../../hooks/shellcheck.sh"
      shell: bash
