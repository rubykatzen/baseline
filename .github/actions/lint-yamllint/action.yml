name: yamllint
description: Lint YAML files using installed yamllint and the canonical baseline config.
runs:
  using: composite
  steps:
    - run: yamllint -c "${{ github.action_path }}/../../../config/yamllint.yml" .
      shell: bash
