name: ruff
description: Lint Python files using installed ruff and the canonical baseline config.
runs:
  using: composite
  steps:
    - run: ruff check --config "${{ github.action_path }}/../../../config/ruff.toml" .
      shell: bash
