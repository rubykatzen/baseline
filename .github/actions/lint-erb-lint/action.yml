name: lint-erb-lint
description: Lint ERB templates using installed Ruby gems and rubykatzen-baseline project stubs.
runs:
  using: composite
  steps:
    - run: "${{ github.action_path }}/../../../hooks/erb-lint.sh"
      shell: bash
