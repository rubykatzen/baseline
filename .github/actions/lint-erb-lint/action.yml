name: erb_lint
description: Lint ERB templates using erb_lint. Requires erb_lint gem in project Gemfile.
runs:
  using: composite
  steps:
    - run: bundle exec erb_lint --lint-all
      shell: bash
