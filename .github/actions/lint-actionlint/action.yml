name: actionlint
description: Lint GitHub Actions workflow files using installed actionlint.
runs:
  using: composite
  steps:
    - run: "${{ github.action_path }}/../../../hooks/actionlint.sh"
      shell: bash
